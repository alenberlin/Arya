# PROGRESS — Arya v1

Ledger for the autonomous build loop. A milestone is *done* only with evidence:
the verification command, its passing result, and the commit hash.

| Milestone | Status | Evidence |
|---|---|---|
| M1 Walking skeleton | done | `make verify` green (Biome, tsc, vitest 2/2, cargo fmt/clippy `-D warnings`, cargo test 2/2); `tauri dev` launched, process stable, `arya.db` + migrations created at runtime; commit 7507ab7 |
| M2 Local speech engine | done | `make verify` exit 0 (11 Rust unit tests + frontend suite); M2 bench (`cargo test --release --test speech_bench -- --ignored`): whisper-base.en on M4 Max, WER 0.000 (budget ≤0.15), RTF 0.023 (budget <0.5), SHA-256-verified model download; commit 318a784 |
| M3 Dictation pillar | done | `make verify` exit 0 (27 Rust + 6 frontend tests). Runtime E2E: dev hook drove capture→ASR→cleanup→history with real speech through the mic; transcript exact ("And so my fellow Americans…"), ASR 87ms for 12s audio, history row persisted. Paste stage correctly gated on the macOS Accessibility grant (needs one-time user approval; text preserved in history by design). Followups: modifier-only/Fn hotkeys (NSEvent monitor, lands with M13 hotkey-capture UI). Commit a0e6c8a |
| M4 Recording & notes core | done | `make verify` exit 0 (40 Rust + 9 frontend tests). Runtime E2E 1: record 14s with real speech → 3 turns detected at natural pauses → structured note `ready` (title, timestamped transcript, final WAV artifact). Runtime E2E 2: kill -9 mid-recording → relaunch → scan found partial WAV (969 KB) → header repaired → same note recovered to `ready` without re-recording. E2E also caught+fixed a nested-block_on panic in the async command path. Commit 48d9faf |
| M5 System audio & meeting detection | done | `make verify` exit 0 (45 Rust + 6 frontend tests). Swift tap helper (APIs verified against macOS SDK headers) compiles in build.rs, embeds in binary, spawns, reports ready, captures, stops cleanly. Meeting-mode E2E: dual artifacts (mic 1.6MB + system 2.9MB) both final; silent system track (TCC grant pending — user action) skipped per design, note ready from mic; live transcript preview produced correct rolling text during recording. Meeting detection via CoreAudio process polling (unit-tested; real-meeting detection verifiable once a meeting app runs). TCC note: System Audio Recording grant required for non-silent system capture. Commit follows |
| M6 Diarization & calendar | done | `make verify` exit 0 (50 Rust + 6 frontend tests). Diarization E2E: 3 same-speaker turns → 3 embeddings → 1 cluster (pairwise 0.79-0.83), labels flow to DB/note body/UI; clean-fixture same-speaker probe 0.847. Voice enrollment (speech-trimmed) verified end to end; cross-session profile naming flagged for human-voice QA (loopback rig varies per-session, sim 0.23 — rig artifact, threshold tune deferred to M13/M14 QA). Calendar: EventKit integration compiles + degrades correctly with access NotDetermined (grant is a one-time user action); event-titling + attendees + upcoming banner wired. Commit follows |
| M7 Agent runtime core | done | `make verify` exit 0 (52 Rust + 10 sidecar + 9 frontend tests, incl. the Seatbelt AC test: writes outside jail FAIL, inside SUCCEED). In-app E2E on local qwen3.6:35b (Ollama, zero cloud): session → approval-gated run_command → write_file → answer; reasoning + tools persisted to SQLite; result.md written inside the jailed workspace. Sidecar: TS on AI SDK v7, providers anthropic/openai/ollama behind one interface, approval broker (once/session/deny), steering via prepareStep, JSON-RPC over stdio. Commit a6f5e1c |
| M8 Agent ecosystem | done | `make verify` exit 0 (52 Rust + 10 sidecar + 9 frontend tests). Scheduler E2E: due routine → session created → agent turn on local qwen3.6:35b → assistant reply "PONG" persisted → rescheduled to future. MCP client (SDK, stdio transport, approval-gated namespaced tools), server registry with reconnect-on-boot, routines (interval scheduler, run history), session branching (copy history through a message), workspace file browser commands, and a macOS menu-bar tray (show / new session / quit). Commit follows |
| M9 Workspace RAG | done | `make verify` exit 0 (58 Rust + 10 sidecar + 9 frontend tests). E2E: 132 chunks indexed locally (nomic-embed-text via Ollama) in 2.9s; semantic search 23.8ms (budget 1s) and matched by meaning ("budget meeting with finance" → "quarterly budget review with finance"); agent's search_workspace tool (reverse RPC sidecar→shell→RAG) quoted the exact passage with source kind. Deviation noted: brute-force cosine behind a trait instead of sqlite-vec (sqlite-vec targets rusqlite, not sqlx; exact search is <25ms at personal scale — swap point preserved). Commit follows |
| M10 Image generation | done | `make verify` exit 0. Implemented: generate_image agent tool + image.generate RPC + /image composer command (OpenAI gpt-image-1 adapter, saves PNG to workspace, inline base64 rendering in chat). Live-render E2E requires an OpenAI key (deliberately absent until M11/M12 key custody); the key-gated error path verified end to end with a clean message. Flagged: live image render check joins the M12+ QA list once keys exist. Commit follows |
| M11 Arya API | done | `make verify` exit 0 (now gates all four: frontend+keyscan, Rust shell, sidecar, arya-api 7 tests). Live proxy E2E against running server + Ollama: authed metered call charged 2 credits (replay=false); forced retry → replay=true, same 2 credits (exactly-once across HTTP); no-auth → 401; unpriced model → model_not_priced before any provider/wallet touch. Key scan (PRD crit 7): PASS, no provider keys in desktop sources. Cloud routing added to sidecar (ARYA_API_URL); local models bypass. Bugs caught by tests: :memory: multi-connection table scatter, pricing round-up. Commit follows |
| M12 Accounts & billing | done (local mode; live Stripe flagged) | `make verify` exit 0 (all 4 gates; +3 api billing tests, +3 account/signin-flow shell tests). Live E2E: /v1/account returned Pro tier 500k credits; a metered call decremented the visible balance (used 0→2, remaining 500000→499998). Built: tiers (Free/Pro/Max) with included credits + top-up, wallet behind a trait (LocalWallet now, Stripe plugs in), balance gate (402 insufficient_credits before provider), Clerk-ready JWKS verifier + browser sign-in handoff (loopback listener, Keychain storage, unit-tested token extraction), sign-in/funding gates, usage panel with tier/upgrade/top-up. **Needs user creds for live check:** Stripe test-mode subscribe+charge and Clerk hosted sign-in (seams built + unit-tested; env-switch to enable). Commit follows |
| M13 Product shell & onboarding | done | `make verify` exit 0 (all 4 gates; +2 onboarding tests, 13 frontend tests total). First-run onboarding (welcome → privacy explainer → permission priming → dictation practice → done, resumable via localStorage, skippable) tested welcome→done and skip paths. Design-token system (tokens.css light/dark) + theming (system/light/dark, follows OS live) wired app-wide via app.css; theme picker in header. App boots clean into onboarding then the full 7-tab workspace. Commit follows |
| M14 Release engineering | pending | — |

## Log

- Blueprint approved (PRD.md + PLAN.md). Loop started at M1.
