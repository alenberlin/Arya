# Arya v1 — autonomous loop handoff

**State:** Dark Factory Phase 1 in progress. Blueprint approved (docs/darkfactory/PRD.md + PLAN.md). Ledger: docs/darkfactory/PROGRESS.md (source of truth — read it first).

**Done:** M1 (7507ab7 skeleton), M2 (318a784 whisper, WER 0/RTF 0.023), M3 (a0e6c8a dictation), M4 (48d9faf recording+notes+recovery), M5 (2dd0fce system audio + meeting detect + live preview), M6 (521ad0c diarization + calendar), M7 (a6f5e1c agent runtime, sidecar, Seatbelt jail).

**Done (cont):** M8 (ac61392 MCP+routines+branch+tray), M9 (d715905 local RAG + agent search tool), M10 (a408346 image gen), M11 (edad360 arya-api metering proxy, live-proven exactly-once).

**Next:** M12 accounts & billing (Clerk auth + Stripe, build in local mode behind seams — live Stripe test-mode charge needs user creds, flag it), M13 onboarding+settings+polish, M14 release eng (signing/notarization needs Apple Developer ID — flag it). Do NOT halt for creds; build+test everything creds-free, list the specific live checks in REPORT.md.

**arya-api:** `arya-api/` crate (lib+bin). `make verify` now gates 4: front+keyscan, rust shell, sidecar, arya-api. Live proxy: `cargo run --manifest-path arya-api/Cargo.toml` (local mode, token local-dev-token, bind 127.0.0.1:8477). Sidecar cloud routing via ARYA_API_URL env.

**Sidecar:** `sidecar/` TS on AI SDK v7 (esbuild → dist/index.mjs), spawned per write-mode by src-tauri/src/agent/sidecar.rs. Dev hook ARYA_DEV_AGENT="<prompt>" + ARYA_DEV_AGENT_MODEL runs one auto-approved turn. Local model available: `ollama:qwen3.6:35b`. Rebuild sidecar after edits: `pnpm --filter arya-sidecar build`.
**More dev hooks:** ARYA_DEV_ENROLL=name:secs, ARYA_DEV_CALENDAR=1.

**Key facts:**
- `make verify` from repo root is the gate; never pipe gates through tail without pipefail.
- Dev E2E hooks (debug-only, env-gated in src-tauri/src/lib.rs dev_hooks): ARYA_DEV_DICTATE_MS, ARYA_DEV_RECORD_MS, ARYA_DEV_RECORD_FOREVER, ARYA_DEV_RECOVER. Pattern: launch `pnpm tauri:dev` bg, `afplay /tmp/arya-fixtures/jfk.wav` when log shows started, check SQLite at `~/Library/Application Support/dev.arya.app/arya.db`.
- Whisper model pre-seeded at `~/Library/Application Support/dev.arya.app/models/ggml-base.en.bin`.
- TCC blockers needing one-time human grants (documented, not code bugs): Accessibility (dictation paste), Screen & System Audio Recording (M5 helper). Mic already granted to terminal.
- Never call tauri::async_runtime::block_on inside async commands (nested runtime panic — bit us once).
- cpal 0.18 (description().name(), SampleRate=u32, config by value), rubato 0.16, whisper-rs 0.16 (get_segment/as_iter API).
- User email for commits: cloud.expert2018@gmail.com, Co-Authored-By: Claude Fable 5.
